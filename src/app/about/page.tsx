import Layout from 'components/layouts/default'

export const metadata = {
    title: "About",
    description: "About Program Adagrad"
}

export default () => {
    return (
        <Layout>
            "Page"
        </Layout>
    )
}