import Layout from 'components/layouts/default'

export const metadata = {
    title: "Blog",
    description: "Program Adagrad Blog"
}

export default () => {
    return (
        <Layout>
            "Page"
        </Layout>
    )
}